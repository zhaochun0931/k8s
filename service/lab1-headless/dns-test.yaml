apiVersion: v1
kind: Pod
metadata:
  name: manual-dns-tester
spec:
  containers:
  - name: tester
    image: alpine:latest
    # This keeps the pod running forever so you can log in manually
    command: ["sh", "-c", "apk add --no-cache bind-tools && sleep 3600"]
  restartPolicy: Never
