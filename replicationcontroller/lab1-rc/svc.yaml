apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-app
  name: my-rc
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: my-app
  type: NodePort
