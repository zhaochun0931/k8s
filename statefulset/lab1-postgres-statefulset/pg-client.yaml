apiVersion: v1
kind: Pod
metadata:
  name: postgres-client
spec:
  containers:
  - name: alpine-client
    image: alpine:latest
    # Install the client and keep it alive
    command: ["sh", "-c", "apk add --no-cache postgresql-client && sleep 3600"]
