---
apiVersion: v1
data:
  student_age: "10"
  student_name: xiaoming
kind: ConfigMap
metadata:
  name: configmap-xxx

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: configmap-demo
  name: configmap-demo
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: configmap-demo
  template:
    metadata:
      labels:
        app: configmap-demo
    spec:
      containers:
      - image: alpine
        imagePullPolicy: Always
        name: alpine
        stdin: true
        tty: true
        env:
          - name: ENV1
            valueFrom:
              configMapKeyRef:
                name: configmap-xxx
                key: student_name
          - name: ENV2
            valueFrom:
              configMapKeyRef:
                name: configmap-xxx
                key: student_age


# # kubectl exec -it configmap-demo-7848dd95df-jr6p4 -- env | egrep -i env
# ENV1=xiaoming
# ENV2=10
# #


---
# kubectl create configmap configmap-demo --from-literal=student_name=xiaoming --from-literal=student_age=10 --dry-run=client -o yaml
  
