apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-kubia
  name: kubia-clusterip
  namespace: default
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: my-kubia
  type: ClusterIP
